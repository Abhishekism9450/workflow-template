---
- name: Fetch the generated dummy value
  hosts: localhost
  gather_facts: false

  tasks:
    - name: Simulate fetching value (ideally passed via extra_vars or stored)
      debug:
        msg: "Fetched Value: {{ dummy_value | default('no value passed') }}"
